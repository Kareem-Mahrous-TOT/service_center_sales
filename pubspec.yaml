name: service_center_sales
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  dartz: ^0.10.1
  dio: ^5.4.3+1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5
  get_it: ^7.7.0
  shared_preferences: ^2.2.3
  mobile_components:
    git:
      url: https://github.com/TOTPLATFORM/mobile_components.git
      ref: dev
  go_router: ^14.1.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.1
  flutter_hooks: ^0.20.5
  fluttertoast: ^8.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
